/**
 * Cross-context port for adapters that fetch work items from external forges.
 */
export type {
  NormalizedSourceItem,
  SourceAdapter,
  SourceGetItemInput,
  SourceGetItemResult,
  SourceListInput,
  SourceListResult,
} from "./source-types";
export { SourceAdapterRegistry } from "./source-types";
